type Farm {
    id: ID
    name: String
    address: String
    coops: [Coop]
}

type Coop {
    id: ID
    name: String
}

type Query {
    getFarm(id: ID, at: Float): Farm
    getFarms(name: String, at: Float): [Farm]
}
