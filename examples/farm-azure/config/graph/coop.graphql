type Coop {
    id: ID
    farmId: String
    name: String
    capacity: Int
    farm: Farm
    hens: [Hen]
}

type Farm {
    id: ID
    name: String
}

type Hen {
    id: ID
    name: String
}

type Query {
    getCoop(id: ID, at: Float): Coop
    getCoops(name: String, at: Float): [Coop]
    getCoopsByFarm(id: ID, at: Float): [Coop]
}
