scalar Date

# Business event (verb). Immutable; front ends POST these. Queryable for audit.
type Query {
  getById(id: ID, at: Float): BuildFarm
  getAll(at: Float): [BuildFarm]
}

type BuildFarm {
  id: ID
  name: String!
  farm_type: String
  zone: String
  owner: String
}
