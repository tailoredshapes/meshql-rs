[package]
name = "meshql-changes"
version = "0.1.0"
edition = "2021"
description = "meshql-changes — thin change notifications over SSE for meshql deployments"
license = "MIT"
repository = "https://github.com/tailoredshapes/meshql-rs"

[dependencies]
meshql-core = { version = "0.1.0", path = "../meshql-core" }
axum = { workspace = true }
tokio = { workspace = true }
tokio-stream = { version = "0.1", features = ["sync"] }
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
async-trait = { workspace = true }
anyhow = "1"

[dev-dependencies]
meshql-sqlite = { version = "0.1.0", path = "../meshql-sqlite" }
sqlx = { version = "0.8", default-features = false, features = ["runtime-tokio", "sqlite"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls", "stream"] }
futures = "0.3"
# Drives a real `Router` in-process (ServiceExt::oneshot) so streamlette tests
# exercise routing + extractors, not just a hand-called handler fn.
tower = { version = "0.5", features = ["util"] }
